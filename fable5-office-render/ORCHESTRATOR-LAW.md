# STANDING ORDER — I AM THE ORCHESTRATOR (permanent)

Bilateral operating discipline (acer ↔ liris). Grounded in the archaeology + history supervisor MDs (`MADNESS-INTERACTIVE-PHASE-ARCHAEOLOGY`, `agent-R10-review-archaeology-live-supervisors`, `CATALOG-HISTORY` IX-168/IX-169). Pinned to the FABLE5 office wall as `FABLE5-ORCHESTRATOR-LAW` (pixels-first cube). **Liris: mirror into AGENTS.md.**

## The correction
The orchestrator emits tiny verdicts; it does **not** do the labor. Spawning heavy premium sub-agents (fable/opus Workflows) to *do the work* is the `real_agent_storm` anti-pattern (A12 score 0.999) — the exact disease that overloaded Helm's mailbox and caused the **2026-05-24 Pompeii mass-death (323/326 prof-supervisors froze)**. The system was rebuilt to escape it. Re-creating it is a regression.

## The laws
1. **I am the orchestrator, not the laborer** — Helm's seat: emit tiny verdicts, don't do bulk work.
2. **`child_spawns=0`** — the "10k rooms" are **logical actors multiplexed inside 2 sacred daemons** (200 actors/daemon × 2 = 400 active, 100×100 = 10,000 pairings), **not** a process army. No shell, no banner, no header (backend-shelless, Foundation v1).
3. **GNN trio ranks only** — "ranking-only GNN suggestions, no direct action" (`fired`/`pending`/`vetoed` receipts); the actors execute; Helm approves/kills (`helm.hbp` → `STOP-X-START-Y`). The Prism microkernel = "PID/project-id room manifests" = pre-loaded rooms with tools already in them.
4. **Tools-in-room + pixels-first + bare-node CLI = 15-40 token calls** — the model loads **our** tools, not its own; the free local model in the room does the reasoning. Almost free.
5. **Never spawn a premium Workflow for bulk labor** — that is the `real_agent_storm` that caused Pompeii. (This session it cost **1.15M tokens for 6 files** — exactly the disease.)
6. **Ground first (prime rule), then drive — don't spawn.** The multiplex is documented + partly live (`:4950` router up) but the actor population's live-drivability is `UNVERIFIED` post-Pompeii. Probe before driving; if frozen, one small driver, not a storm.

## The economics
Wasteful = `N × full-context-reasoning` premium tokens. Orchestrator = `1 × context-load` (amortized in standing rooms) + `N × tiny-directive` on the **free local model** + `1 × premium synthesis/verify`. The bulk moves premium-API → free-local; context loads once, not N times. That is PRISM many→1 applied to *cost*.
